import java.time.Instant;
import java.time.temporal.ChronoUnit;

public class WarpCLI {
    private static final long START_EPOCH = {{ start_epoch }}L;

    public static void main(String[] args) {
        Instant now = Instant.now();
        Instant start = Instant.ofEpochSecond(START_EPOCH);
        Instant end  = start.plus(1, ChronoUnit.DAYS);

        if(now.isAfter(start) && now.isBefore(end)) System.out.println({{ obf }});
        else System.out.println("It's not your time ;)");
    }
}
